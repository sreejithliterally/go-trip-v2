/**
 * Swagger route docs have been moved to each module's own .routes.js file.
 * swagger-jsdoc scans src/modules/**\/*.routes.js automatically via the
 * apis glob in swagger.js, so no changes are needed here when adding routes.
 */
